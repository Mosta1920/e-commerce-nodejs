// System roles
export const systemRoles = {
    USER: 'user',
    ADMIN: 'admin',
    SUPER_ADMIN: 'super-admin',
    DELIVERY_ROLE: 'deliever',
    // [Symbol.iterator]: function* () {
    //   const values = Object.values(this);
    //   for (let i = 0; i < values.length; i++) {
    //     yield values[i];
    //   }
    // },
  };